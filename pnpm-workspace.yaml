packages:
  - "packages/*"
  - "apps/*"

# 依赖安装校验会在脚本执行前触发，此处关闭以免阻塞 test / typecheck
verifyDepsBeforeRun: false
allowBuilds:
  esbuild: true
  # Electron 的 postinstall 负责下载那个 ~100MB 的运行时。被拦下的话
  # 包会装上但跑不起来，报 "Electron failed to install correctly"。
  electron: true
